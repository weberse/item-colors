var React = require('react');

var Colors = React.createClass({

        render: function () {
            return (
                <div>OK</div>
            );
        }
});

module.exports = Colors;